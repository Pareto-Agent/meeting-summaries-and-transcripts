.DS_Store
*.pdf
*.PDF

# Raw recording artifacts (large media / Zoom/Dropbox drops) — not for git
*.mp4
*.m4a
*.mov
*.vtt
Meeting Transcription.txt

# Gemini-fetcher machine state (which Drive docs were already pulled)
transcripts/.drive-fetch-state.json

# Raw auto-fetched files awaiting canonicalization by /update-wiki
transcripts/_inbox/
